import { ModelLearningDirective } from './model-learning.directive';

describe('ModelLearningDirective', () => {
  it('should create an instance', () => {
    const directive = new ModelLearningDirective();
    expect(directive).toBeTruthy();
  });
});
